---
- name: K3s Cluster Installation
  hosts: all
  tasks:
    - name: Installing Components
      ansible.builtin.include_role:
        name: "{{ deploy_type }}"
      loop: "{{ applications }}"
      when: "item.name == {{ 'app' }}"
